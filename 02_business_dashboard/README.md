# Dashboard follow-up

Dashboard ini tidak melatih model. Ia membaca file `Scoring_Customer_Aktif_Lengkap.xlsx` dari notebook, lalu menampilkan prioritas follow-up.

Pada Windows, klik `start_dashboard_windows.bat`. Browser akan membuka `http://localhost:8501`.

File contoh untuk tes dashboard ada di `../data/template/Scoring_Customer_Aktif_Contoh.xlsx`.
